n=int(input())
ph={}
for i in range (n):
	cmd=input().split()
   if cmd[0]=="ADD":
		ph[cmd[i]]=cmd[2]
   elif cmd[0]=="REMOVE":
       ph.pop(cmd[i],None)
   elif cmd[0]=="DISPLAY":

 if len(ph)==0:
	print("No contacts")
 else 
   for name in sorted (ph)
	 print(f"{name}:{ph[name]}")
